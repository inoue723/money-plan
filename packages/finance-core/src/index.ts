/**
 * finance-core — 資産推移シミュレーションの計算エンジン(UI非依存)。
 *
 * 本パッケージには税・社会保険料・教育費・投資運用などの純粋関数を実装していく。
 * 現時点は monorepo の workspace 依存疎通を確認するためのダミー関数のみ。
 */

/** workspace 依存の疎通確認用ダミー関数。 */
export const ping = (): string => 'pong';
